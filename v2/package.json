{
  "name": "mandate-v2",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Mandate rebuilt: five primitives, one event log. See ../README.md and v2/README.md.",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "scripts": {
    "dev": "node server/index.js",
    "test": "node --test server/test/*.test.js",
    "build": "npm run build -w web"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "@hono/node-server": "^1.19.9",
    "hono": "^4.12.1",
    "zod": "^4.3.6"
  }
}
